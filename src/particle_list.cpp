#include "particle_list.h"

namespace tweezers
{
  /*
  particle_list::particle_list(const size_t& num_particles) : _num_particles(num_particles) {}
  
  const size_t& num_particles() { return _num_particles; }
  const size_t& num_particles() const { return _num_particles; }
  */
	/*particle_list::particle_list(int size)
	{
		this->size = size;
		pos = new float3[size];
		vel = new float3[vel];
		acc = new float3[acc];
	}

	particle_list::~particle_list()
	{
		delete[] pos;
		delete[] vel;
		delete[] acc;
	}

	bool particle_list::make_particle(int idx float x, float y, float z)
	{
		if(idx < 0 || idx >= this->size) return false;

		pos[idx].x = x;
		pos[idx].y = y;
		pos[idx].z = z;
		
		vel[idx].x = vel[idx].y = vel[idx].z = 0;
		acc[idx].x = acc[idx].y = acc[idx].z = 0;

		return true;
	}*/
}

